FOR p1 = 10^6-1 TO 5 STEP -2

   IF prime(p1) = 1 THEN
      IF p2 <> 0 THEN 
       
         LET v = 0
         DO
            LET v = v + 1
            IF MOD(v*p2-p1 , 10^(LEN(STR$(p1))-1)) = 0 THEN EXIT DO
         LOOP
          
         LET s = v*p2
         LET sums = sums + s
          
          
      END if
      LET p2 = p1
   END IF
    
NEXT p1


PRINT sums

FUNCTION prime(n)
   local i
   IF MOD(n,2)=0 THEN
      IF n=2 THEN LET prime = 1 ELSE LET prime = 0
   ELSE
      FOR i=3 TO SQR(n) STEP 2
         IF MOD(n,i)=0 THEN EXIT FOR 
      NEXT i
      IF i<=SQR(n) THEN LET prime = 0 ELSE LET prime = 1
   END IF
END FUNCTION 



END
