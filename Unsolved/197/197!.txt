DEF f(x) = INT(2^(30.403243784-x^2)) * 10^(-9)

LET ui = -1
FOR i = 1 TO 10^12
   LET u = ui 
   LET ui = f(u)
NEXT i

LET answer$ = STR$(u+ui)
FOR i = 1 TO 100
   IF answer$(i:i) = "." THEN EXIT FOR 
NEXT i

PRINT answer$(i-1:i+9) 

END
